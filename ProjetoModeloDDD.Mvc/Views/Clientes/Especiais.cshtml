@model IEnumerable<ProjetoModeloDDD.Mvc.ViewModels.ClienteViewModel>

@{
    ViewBag.Title = "Clientes Especiais";
}

<h2 class="mb-4">Clientes Especiais</h2>

<!-- Botão para criar um novo cliente -->
<p>
    <a href="@Url.Action("Create")" class="btn btn-primary">Criar Novo Cliente</a>
</p>

<!-- Tabela de clientes especiais -->
<table class="table table-striped table-hover table-bordered">
    <thead class="table-dark">
        <tr>
            <th>@Html.DisplayNameFor(model => model.Nome)</th>
            <th>@Html.DisplayNameFor(model => model.Sobrenome)</th>
            <th>@Html.DisplayNameFor(model => model.Email)</th>
            <th>@Html.DisplayNameFor(model => model.Ativo)</th>
            <th>Ações</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Nome)</td>
                <td>@Html.DisplayFor(modelItem => item.Sobrenome)</td>
                <td>@Html.DisplayFor(modelItem => item.Email)</td>
                <td>@Html.DisplayFor(modelItem => item.Ativo)</td>
                <td>
                    <!-- Botões de ação para Editar, Detalhes e Excluir -->
                    <a href="@Url.Action("Edit", new { id = item.ClienteId })" class="btn btn-warning btn-sm">Editar</a>
                    <a href="@Url.Action("Details", new { id = item.ClienteId })" class="btn btn-info btn-sm ms-2">Detalhes</a>
                    <a href="@Url.Action("Delete", new { id = item.ClienteId })" class="btn btn-danger btn-sm ms-2">Excluir</a>
                </td>
            </tr>
        }
    </tbody>
</table>
